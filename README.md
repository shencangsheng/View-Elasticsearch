# View-Elasticsearch

## Compile Dependencies

* maven:3.6.3-openjdk-11

## Run Dependencies

* openjdk-11

## Supported Backends

* Docker mode

## Building from source

uses `Maven` for its build system.
